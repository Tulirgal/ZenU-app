import 'package:flutter/material.dart';

class BubbleScreen extends StatelessWidget {
  const BubbleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('BubbleScreen')));
  }
}
