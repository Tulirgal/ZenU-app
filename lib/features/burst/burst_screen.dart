import 'package:flutter/material.dart';

class BurstScreen extends StatelessWidget {
  const BurstScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('BurstScreen')));
  }
}
