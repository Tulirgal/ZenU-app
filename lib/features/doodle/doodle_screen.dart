import 'package:flutter/material.dart';

class DoodleScreen extends StatelessWidget {
  const DoodleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DoodleScreen'),
      ),
    );
  }
}
