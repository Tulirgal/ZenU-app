import 'package:flutter/material.dart';

class ScribbleScreen extends StatelessWidget {
  const ScribbleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ScribbleScreen'),
      ),
    );
  }
}
