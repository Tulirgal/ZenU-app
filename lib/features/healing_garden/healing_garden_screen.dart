import 'package:flutter/material.dart';

class HealingGardenScreen extends StatelessWidget {
  const HealingGardenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('HealingGardenScreen'),
      ),
    );
  }
}
