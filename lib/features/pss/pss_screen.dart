import 'package:flutter/material.dart';

class PSSScreen extends StatelessWidget {
  const PSSScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('PSSScreen'),
      ),
    );
  }
}
